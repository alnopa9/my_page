Alyssa Argneto's XPath Exercise 2 Answers

Question 1:
    Part A-
        //div[@type="book"][position()=1]/div[@type="chapter"]/p/geo[@select="lat"]
        42 items
    Part B-
        //div[@type="book"][position()=1]/div[@type="chapter"]/p/geo[count(@select="lat"])
Question 2:
    //div[@type="book"][position()=1]/div[@type="chapter"]/p[geo[@select="lat"]]
Question 3:
    XPath expression-
        //div[@type="book"][position()=3]/div[@type="chapter"][position()=1]/p/geo[@select="lat"]
    Paragraph "702" in the file
Question 4:
    //p[geo[@select="lon"]][geo[@select="lat"]]
Question 5:
    XPath expression-
        //p[not(geo[@select="lat"])][geo[@select="lon"]]
    There are 4 paragraphs in the whole document that do not contain a lagitude measurement but do contain a longitude measurement
Question 6:
    //p[geo]/placeName[1] returns all of the first place names that appear in each paragraph that contains a geo element and ignores the rest
    (//p[geo]/placeName)[1] returns only the first place name in the first paragraph that contains a geo element and ignores the rest